/// <reference types="vite/client" />

/** Injected at build time (vite define) — shown on Home so testers can verify
 * the loaded extension matches the code they just built. */
declare const __BUILD_TIME__: string;
