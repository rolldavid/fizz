# Keep the build context small. The image builds only the web SPA (web/) and
# serves web/dist, so the extension build, deps, and generated output are all
# excluded (the web build regenerates web/dist).
**/node_modules
dist
web/dist
.git
*.log
chrome
docs
LOOP_STATE.md
