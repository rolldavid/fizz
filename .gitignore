node_modules
dist
web/node_modules
# The web SPA build output (regenerated by Railway's Dockerfile on deploy).
web/dist
.DS_Store
*.log
.vite
.cache
.env
.env.local
*.tsbuildinfo
chrome/
*.zip
LOOP_STATE.md

# crxjs build artifacts — dist.pem is a signing PRIVATE KEY, never commit
dist.crx
dist.pem

docs
CLAUDE.MD
.claude

# Internal security invariants spec/audit + the verified fix backlog. Contain
# unfixed findings + file:line pointers = an attacker roadmap. KEEP LOCAL.
# The sanitized, leak-checked public versions live at /INVARIANTS.md and
# /SECURITY.md (committed). The two internal files are anchored to root so the
# public docs are never caught by the ignore.
/INVARIANTS.internal.md
/INVARIANTS.fixlist.md

# Playwright e2e artifacts
/playwright-report
/test-results
/blob-report

# Funded e2e accounts — REAL 12-word seed phrases with live tokens. NEVER
# commit. Fill in tests/playwright/.accounts.local.json (the committed
# .accounts.example.json shows the shape), or use PW_ACCOUNT_* env vars.
/tests/playwright/.accounts.local.json
.env.e2e.local