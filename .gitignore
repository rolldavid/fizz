node_modules
dist
# web/ (fizzwallet.com pages) has its own package; only its BUILT output
# (committed into landing/) ships.
web/node_modules
.DS_Store
*.log
.vite
.cache
.env
.env.local
.tsbuildinfo
chrome/
*.zip
LOOP_STATE.md
