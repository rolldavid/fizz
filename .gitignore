node_modules
dist
web/node_modules
web/dist
# Railway builds the web app from web/ on deploy (Dockerfile) and writes these
# into landing/, so the build output is not committed. The static home
# (landing/index.html, images, privacy.html) IS committed.
landing/bridge/
landing/launch/
landing/webassets/
.DS_Store
*.log
.vite
.cache
.env
.env.local
*.tsbuildinfo
chrome/
*.zip
LOOP_STATE.md

# crxjs build artifacts — dist.pem is a signing PRIVATE KEY, never commit
dist.crx
dist.pem

docs