diff --git a/node_modules/@aztec/foundation/dest/json-rpc/client/fetch.js b/node_modules/@aztec/foundation/dest/json-rpc/client/fetch.js
index 867a42d..fbe56aa 100644
--- a/node_modules/@aztec/foundation/dest/json-rpc/client/fetch.js
+++ b/node_modules/@aztec/foundation/dest/json-rpc/client/fetch.js
@@ -38,7 +38,11 @@ const log = createLogger('json-rpc:json_rpc_client');
         throw new Error(`Failed to parse body as JSON: ${await resp.text()}`);
     }
     if (!resp.ok) {
-        const errorMessage = `Error ${resp.status} from server ${host}: ${responseJson.error.message}`;
+        // PATCHED (fizz): non-Aztec error shapes (e.g. drpc rate-limit bodies)
+        // have no `.error.message`; reading it threw a TypeError that masked
+        // the real status. Surface status + body instead.
+        const errorDetail = responseJson?.error?.message ?? JSON.stringify(responseJson).slice(0, 300);
+        const errorMessage = `Error ${resp.status} from server ${host}: ${errorDetail}`;
         if (noRetry || resp.status >= 400 && resp.status < 500) {
             throw new NoRetryError(errorMessage);
         } else {
diff --git a/node_modules/@aztec/foundation/dest/json-rpc/client/safe_json_rpc_client.js b/node_modules/@aztec/foundation/dest/json-rpc/client/safe_json_rpc_client.js
index 07c93b1..784bad3 100644
--- a/node_modules/@aztec/foundation/dest/json-rpc/client/safe_json_rpc_client.js
+++ b/node_modules/@aztec/foundation/dest/json-rpc/client/safe_json_rpc_client.js
@@ -9,7 +9,10 @@ import { defaultFetch } from './fetch.js';
 // minimal latency
 const DEFAULT_BATCH_WINDOW_MS = 0;
 // the maximum size of a batched request
-const DEFAULT_MAX_BATCH_SIZE = 100;
+// PATCHED (fizz): drpc's free tier (the canonical public Aztec mainnet RPC)
+// rejects JSON-RPC batches of more than 3 requests with a 500. The SDK's
+// default of 100 made every deploy/simulation burst fail there.
+const DEFAULT_MAX_BATCH_SIZE = 3;
 // 10 mb
 const DEFAULT_MAX_REQUESTY_BODY_SIZE = 10 * 1024 * 1024;
 // expose helpful information on the RPC clients such that we can recognize them later
@@ -165,7 +168,8 @@ const clients = new Map();
         const response = await deferred.promise;
         log.debug(format(`result`, method, response));
         if ('error' in response) {
-            throw new Error(response.error.message, {
+            // PATCHED (fizz): `error` can be present but null/malformed.
+            throw new Error(response.error?.message ?? JSON.stringify(response.error), {
                 cause: response.error
             });
         }
